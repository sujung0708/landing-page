# 컬러 팔레트 디자인 시스템

## 프로젝트 정보
- **출처 이미지**: 레퍼런스.jpg (3D 마블런 구조물 / 네오팝 스타일)
- **분석 날짜**: 2026-02-24
- **적용 프로젝트**: 포트폴리오 웹사이트

---

## CSS 변수 정의

```css
:root {
  /* ── Primary Colors (핫핑크 계열) ── */
  --color-primary:       #E8447A;   /* 비비드 핫핑크 — 메인 강조 */
  --color-primary-light: #F4B8C1;   /* 연핑크 — 서피스·배경 */
  --color-primary-dark:  #C2185B;   /* 딥 마젠타 — 호버·그림자 */

  /* ── Secondary Colors (퍼플 계열) ── */
  --color-secondary:       #8B5CF6; /* 미디엄 퍼플 — 보조 강조 */
  --color-secondary-light: #A78BFA; /* 라벤더 — 보조 서피스 */
  --color-secondary-dark:  #6D28D9; /* 딥 퍼플 — 보조 호버 */

  /* ── Accent Colors ── */
  --color-accent:         #FF4500;  /* 코랄레드 — 강한 포인트 */
  --color-accent-soft:    #FFAA8A;  /* 피치 살몬 — 부드러운 포인트 */
  --color-accent-mist:    #FFCABA;  /* 연피치 안개 — 섹션 구분 */

  /* ── Gradient Stops ── */
  --color-gradient-start: #8B5CF6;  /* 퍼플 */
  --color-gradient-mid:   #D946A8;  /* 핑크-퍼플 */
  --color-gradient-end:   #E8447A;  /* 핫핑크 */

  /* ── Background Colors ── */
  --color-bg-primary:   #D6E4F0;    /* 하늘 연보라 — 메인 배경 */
  --color-bg-secondary: #F5F5F5;    /* 흰 구름 — 카드·섹션 배경 */
  --color-bg-surface:   #FFFFFF;    /* 순백 — 모달·팝업 */
  --color-bg-sky:       #3B5BDB;    /* 딥 스카이블루 — 다크 섹션 */

  /* ── Text Colors ── */
  --color-text-primary:   #1A1A2E;  /* 다크 네이비 — 본문·제목 */
  --color-text-secondary: #4A4A6A;  /* 미디엄 그레이-퍼플 — 소제목 */
  --color-text-muted:     #9B9BB0;  /* 뮤트 라벤더 — 캡션·플레이스홀더 */
  --color-text-inverse:   #FFFFFF;  /* 흰색 — 어두운 배경 위 텍스트 */

  /* ── Border / Outline ── */
  --color-border:         #E2D9F3;  /* 연보라 보더 — 카드·구분선 */
  --color-border-strong:  #8B5CF6;  /* 퍼플 보더 — 포커스·선택 상태 */
  --color-border-accent:  #E8447A;  /* 핫핑크 보더 — 오류·강조 */

  /* ── Interactive Colors ── */
  --color-button-primary:  #E8447A; /* 기본 버튼 배경 */
  --color-button-hover:    #C2185B; /* 버튼 호버 */
  --color-button-secondary: #8B5CF6;/* 보조 버튼 배경 */
  --color-button-ghost:    transparent; /* 고스트 버튼 */
  --color-link:            #8B5CF6; /* 링크 기본색 */
  --color-link-hover:      #E8447A; /* 링크 호버 */
  --color-focus-ring:      #A78BFA; /* 포커스 아웃라인 */

  /* ── Utility ── */
  --color-shadow:    rgba(139, 92, 246, 0.20); /* 퍼플 그림자 */
  --color-overlay:   rgba(26, 26, 46, 0.60);   /* 오버레이 */
  --color-highlight: #B0B8C1;                  /* 크로뮴 실버 — 장식 */
}
```

---

## 핵심 그라데이션 스니펫

```css
/* 메인 히어로 그라데이션 */
.gradient-main {
  background: linear-gradient(135deg,
    var(--color-gradient-start) 0%,
    var(--color-gradient-mid)   50%,
    var(--color-gradient-end)   100%
  );
}

/* 배경 하늘 그라데이션 */
.gradient-sky {
  background: linear-gradient(180deg,
    var(--color-bg-sky)     0%,
    var(--color-bg-primary) 40%,
    var(--color-bg-secondary) 70%,
    var(--color-accent-mist) 100%
  );
}

/* CTA 버튼 그라데이션 */
.btn-gradient {
  background: linear-gradient(90deg,
    var(--color-primary) 0%,
    var(--color-secondary) 100%
  );
}
```

---

## 컬러 사용 가이드

### Primary (`#E8447A` 핫핑크)
- 메인 CTA 버튼, 섹션 제목 언더라인
- 강조 텍스트, 배지, 태그
- 활성 탭·메뉴 인디케이터

### Secondary (`#8B5CF6` 퍼플)
- 링크, 아이콘, 보조 버튼
- 카드 호버 보더, 포커스 링
- 코드 블록 배경 강조

### Accent (`#FF4500` 코랄레드)
- 오류 메시지, 경고 아이콘
- 특별 프로모션 배지
- 3~5% 이하로 극제한 사용

### Background (`#D6E4F0` 연보라 하늘)
- 전체 페이지 기본 배경
- 짝수 섹션 배경색으로 교차 활용

### Text (`#1A1A2E` 다크 네이비)
- 모든 본문 텍스트
- `--color-bg-primary` 위에서 WCAG AA 대비율 충족

---

## 반응형 고려사항

### 다크 모드

```css
@media (prefers-color-scheme: dark) {
  :root {
    --color-bg-primary:     #0F0F1A;  /* 딥 다크 네이비 */
    --color-bg-secondary:   #1A1A2E;  /* 다크 카드 배경 */
    --color-bg-surface:     #252540;  /* 다크 모달 */

    --color-text-primary:   #F0EEFF;  /* 연보라 흰색 */
    --color-text-secondary: #C4B5FD;  /* 라벤더 */
    --color-text-muted:     #7C7CA0;

    --color-border:         #3A2F6A;  /* 다크 퍼플 보더 */

    /* Primary / Secondary / Accent는 그대로 유지 — 채도 유지 */
  }
}
```

### 고대비 모드

```css
@media (prefers-contrast: high) {
  :root {
    --color-primary:       #D50057;  /* 더 진한 핑크 */
    --color-secondary:     #6200EA;  /* 더 진한 퍼플 */
    --color-text-primary:  #000000;
    --color-border-strong: #000000;
  }
}
```

### 화면 크기별 권장 사항

| 화면 | 적용 포인트 |
|------|------------|
| **Mobile** (< 768px) | `--gradient-main` 방향을 `180deg`로 전환, 배경 단색 fallback 사용 |
| **Tablet** (768–1024px) | 카드 보더에 `--color-border` 유지, 그라데이션 hero 유지 |
| **Desktop** (> 1024px) | 풀 그라데이션 + 파티클·3D 효과 모두 활성화 |

---

## 컬러 조합 예시

```css
/* 히어로 섹션 */
.hero {
  background: linear-gradient(180deg, #3B5BDB 0%, #D6E4F0 50%, #FFCABA 100%);
  color: var(--color-text-inverse);
}

/* 주요 카드 */
.card {
  background: var(--color-bg-secondary);
  border: 1px solid var(--color-border);
  box-shadow: 0 8px 32px var(--color-shadow);
}
.card:hover {
  border-color: var(--color-border-strong);
}

/* CTA 버튼 */
.btn-primary {
  background: var(--color-button-primary);
  color: var(--color-text-inverse);
}
.btn-primary:hover {
  background: var(--color-button-hover);
}
```

---

> **디자인 무드**: 네오팝 · Y2K · 몽환적 3D — 젊고 역동적이며 창의적인 포트폴리오 브랜드
